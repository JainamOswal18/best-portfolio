# HTTP port the server listens on
PORT=8080

# Allowed CORS origin for the frontend
FRONTEND_URL=http://localhost:5173

# Gemini API key (leave blank to disable AI endpoints — they return a stub)
GEMINI_API_KEY=

# SMTP settings for the contact form (leave SMTP_HOST blank for dev mode — logs only)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_TO=jainamoswal1811@gmail.com
MAIL_FROM=portfolio@jainamoswal.com
